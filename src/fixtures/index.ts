/* This file should be filled with the appropriate fixtures. To prevent webpack from bundling the fixtures in the
   production build (which it will if it encounters a import/require [someFixture.json], even if it is conditional),
   it should be filled before running storybook / tests etc.
 */

export default {} as any;
