version: '3'

services:
  admin:
    image: node:9.11.1-alpine

  test:
    image: rsdnlesc/tests
    build:
      context: ./tests
    volumes:
      - /var/lib/docker.sock:/var/lib/docker.sock
    depends_on:
      - mongo
      - backend
      - frontend
      - auth
      - nginx
